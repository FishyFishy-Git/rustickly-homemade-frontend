import './App.css';
import {
  createBrowserRouter,
  createRoutesFromElements,
  Route,
  RouterProvider
} from "react-router-dom"

//import pages

const router = createBrowserRouter()

function App() {
  return (
    <div>placeholder</div>
  );
}

export default App;
